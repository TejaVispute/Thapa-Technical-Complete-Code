// The DOM is the Document Object model, which deals with the document., the HTML elements themselves, ex document and all traversal you would do in it, events, etc

// For Ex. 

// change the background color to red

// document.body.style.background= "red";


// The BOM is the browser Object model deals with browser components aside from the document, like history, location, navigator and screen (as well as some others that vary by browser) OR
// In simple eaning all the window operations which comes under BOM are prefered using BOM

// Practice Time 
// How to find child in DOM
// firstChild  Vs last ElementChild
// const data = document.body.firstElementChild

// undefined

    // data.firstElementChild
    // data.firstElementChild.firstElementChild
    

    // Now the same we have is the query selector 
    // queryselector returns the first matching value wheres queryselectorAll will retur all the elements.


    // Interview Question 

    // What is difference between getElemtmentbyID  and queryselector


    // What is gerElementById()?

    // syntax : 
    // element= document.getElementById(id);

    // Returns a refrence to the element by its IDBCursor.
    // If the element with specified ID is not in the document, it will return null.

    // What is queryselector() ?

    // syntax:

    // element = document.queryselector(selector);

    // Returns the first element within the document that matches the specified goup of selector or null if no matches are found

    

